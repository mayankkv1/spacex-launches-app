import { AddPlaceholderPipe } from './add-placeholder.pipe';

describe('AddPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new AddPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
